/**
 * AppShell Component
 * 
 * Purpose: Main app layout wrapper (sidebar + topnav + content)
 * Usage: <AppShell><YourContent /></AppShell>
 */

export { default } from './AppShell'
export { default as AppShell } from './AppShell'
