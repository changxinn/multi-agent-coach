/**
 * Notification Popup Component
 * 
 * Purpose: Display notifications popup when bell icon is clicked
 * Usage: <NotificationPopup isOpen={isOpen} onClose={handleClose} />
 * 
 * Features:
 * - Click outside to close
 * - Mark as read functionality
 * - Unread count badge
 * - Type-based styling (info, warning, success, error)
 */

import NotificationPopup from './NotificationPopup'

export { NotificationPopup } from './NotificationPopup'
export default NotificationPopup
